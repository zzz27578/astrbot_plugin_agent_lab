{
  "private_only": {
    "type": "bool",
    "description": "仅允许私聊使用",
    "default": true,
    "hint": "第一版建议开启，避免群聊误触发、权限扩散和上下文混杂。"
  },
  "standalone_webui_enabled": {
    "type": "bool",
    "description": "启用独立 Agent Lab 控制台",
    "default": true,
    "hint": "启动插件自带 Web 服务，不再依赖 AstrBot Dashboard 插件 Page。"
  },
  "standalone_webui_host": {
    "type": "string",
    "description": "独立控制台监听地址",
    "default": "127.0.0.1",
    "hint": "默认仅本机可访问；如需局域网访问可改为 0.0.0.0，并务必设置访问 Token。"
  },
  "standalone_webui_port": {
    "type": "int",
    "description": "独立控制台监听端口",
    "default": 8788,
    "hint": "访问地址默认为 http://127.0.0.1:8788"
  },
  "standalone_webui_token": {
    "type": "string",
    "description": "独立控制台访问 Token",
    "default": "",
    "hint": "为空时不校验 API Token；建议公网或局域网监听时填写。"
  },
  "inject_agent_mode_policy": {
    "type": "bool",
    "description": "注入 Agent Mode 自主判断协议",
    "default": true,
    "hint": "开启后 bot 可根据用户自然语言与 Agent 配置判断是否进入 Agent Mode。"
  },
  "install_agent_mode_skill": {
    "type": "bool",
    "description": "安装 agent-mode skill",
    "default": true,
    "hint": "把 Agent Mode 的触发、审批、心跳和归档规范同步为 AstrBot Skill。"
  },
  "entry_summary_turns": {
    "type": "int",
    "description": "入口摘要读取最近轮数",
    "default": 24,
    "hint": "进入 Agent Mode 时读取最近对话并压缩成 task_brief。"
  },
  "fallback_summary_provider_id": {
    "type": "string",
    "description": "备用总结模型 Provider ID",
    "default": "",
    "hint": "当前会话模型总结失败时使用；为空则只用当前模型。"
  },
  "max_steps_per_tick": {
    "type": "int",
    "description": "每次 tick 最大工具步数",
    "default": 12,
    "hint": "限制单轮执行范围，避免失控长循环。"
  },
  "tool_call_timeout": {
    "type": "int",
    "description": "工具调用超时秒数",
    "default": 120,
    "hint": "单个工具调用的最大等待时间。"
  },
  "agent_mode_progress_notice_mode": {
    "type": "string",
    "description": "Agent Mode 运行提示模式",
    "default": "agent_lab",
    "options": ["agent_lab", "astrbot", "silent"],
    "hint": "agent_lab=插件按节流策略发送任务进度；astrbot=按 AstrBot 原生工具调用样式逐次提示；silent=只记录任务状态，不主动发送进度/工具提示。"
  },
  "agent_mode_progress_every_tools": {
    "type": "int",
    "description": "插件进度提示工具间隔",
    "default": 3,
    "hint": "仅 agent_lab 模式生效；每累计多少次工具调用允许发送一次插件进度提示，首个工具调用总会提示。"
  },
  "agent_mode_progress_min_interval_seconds": {
    "type": "int",
    "description": "插件进度提示最小秒间隔",
    "default": 45,
    "hint": "仅 agent_lab 模式生效；距离上次进度提示不足该秒数时跳过，填 0 表示只按工具次数节流。"
  },
  "llm_compress_keep_recent": {
    "type": "int",
    "description": "Agent loop 压缩保留最近消息数",
    "default": 6
  },
  "truncate_turns": {
    "type": "int",
    "description": "Agent loop 触发截断时丢弃轮数",
    "default": 2
  },
  "workflow_auto_steps_per_tick": {
    "type": "int",
    "description": "Workflow auto node steps per tick",
    "default": 6,
    "hint": "Limits deterministic canvas node execution before handing control to ReAct."
  },
  "task_lease_ttl_seconds": {
    "type": "int",
    "description": "Task lease TTL seconds",
    "default": 600,
    "hint": "Prevents duplicated heartbeat/manual ticks from running the same task concurrently."
  },
  "max_seconds_per_tick": {
    "type": "int",
    "description": "Max seconds per tick",
    "default": 240,
    "hint": "Watchdog pauses a task when one tick exceeds this runtime budget."
  },
  "max_tokens_per_tick": {
    "type": "int",
    "description": "Max tokens per tick",
    "default": 12000,
    "hint": "Per-tick token budget recorded from provider usage."
  },
  "max_total_ticks": {
    "type": "int",
    "description": "Max total task ticks",
    "default": 120,
    "hint": "Total task heartbeat/manual progress budget."
  },
  "max_total_tool_calls": {
    "type": "int",
    "description": "Max total task tool calls",
    "default": 240,
    "hint": "Total budget for concrete tool/API executions across the task."
  },
  "max_total_tokens": {
    "type": "int",
    "description": "Max total task tokens",
    "default": 240000,
    "hint": "Total token budget across the task."
  }
}
