# Agent Lab

为 AstrBot 机器人提供**持续任务执行**能力的插件。

## 解决什么问题

普通聊天机器人的每条回复都是独立的——它不记得上一步做到哪里，不会主动推进未完成的工作，也没有办法在执行高风险操作前征求你的意见。当你需要 bot 帮你完成一个跨多轮、需要保持状态的复杂任务时（比如排查一个 bug、逐步整理文档、或者按计划执行一组操作），普通对话模式就力不从心了。

Agent Lab 为 AstrBot 补上这块缺失：让 bot 能像一个有交接记录的助手一样，接住任务、保存进度、分步推进、遇到风险操作先问你，直到任务完成归档。

## 特色

**任务不会断** — 每一步执行结果都持久化到本地文件，意外中断或主动暂停后随时可以从上次位置继续，不需要重复描述背景。

**记忆互不干扰** — 任务模式有独立的记忆空间。日常闲聊不会被任务细节污染，任务推理也不会被无关对话带偏。任务结束后产生的经验记忆需要你手动确认才会进入日常可读范围。

**危险操作先审批** — 删除文件、执行部署、读取密钥等高风险动作，bot 会先告诉你打算做什么、有什么影响，等你批准后再执行。审批粒度可以按工具逐个配置。

**可视化工作流** — 独立全屏画布，把任务拆成节点图来编排：入口、工具调用、API 请求、条件分支、并行执行、审批门、出口。拖拽连线即可定义执行逻辑，自带静态检查和预跑诊断。

**心跳自动续跑** — 长时间任务可以开启定时心跳，bot 会被周期性唤醒来检查进度并继续推进，无需你手动 tick。

**工具和插件隔离** — 进入任务模式时可以自动关闭无关插件、收窄可用工具到白名单，退出时按快照恢复，不影响全局配置。

**经验沉淀** — 完成的任务会自动提炼为可复用的任务模板。下次遇到类似任务，bot 可以直接参考历史执行计划，不需要从零思考。

**自定义 API 和凭证** — 在 WebUI 中注册外部 API 和加密凭证，任务模式中通过统一工具调用，凭证自动注入且不会回显密钥。

**外部方案兼容** — 以蓝图形式收束 LangGraph、OpenAI Agents、CrewAI、Microsoft Agent Framework 等外部方案的好设计，把它们的协议翻译成 Agent Lab 可用的约束模块。

## 快速开始

### 安装

在 AstrBot 插件管理中搜索安装，或手动克隆：

```bash
git clone https://github.com/zzz27578/astrbot_plugin_agent_lab.git data/plugins/astrbot_plugin_agent_lab
```

启动后插件自动初始化，无需额外配置即可使用基本功能。

### 基本用法

在私聊中发送命令即可：

```
/al start 帮我排查项目里的单元测试报错
```

bot 会进入任务模式，创建任务状态，开始逐步推进。你可以随时用 `/al tick` 推进一步，或 `/al finish` 归档收工。

也支持自然语言触发：当 bot 判断当前对话适合进入任务模式时，会主动询问是否开启。

### WebUI 控制台

AstrBot 4.24.2 及以上会自动发现插件内的 `_dashboard` 原生页面，可直接从 Dashboard 左侧「插件」菜单进入 Agent Lab；页面复用 AstrBot 管理员登录态，不需要额外开放端口。

为兼容 AstrBot 4.16–4.24.1，插件仍可启动独立控制台：

```
http://127.0.0.1:8788
```

独立控制台默认只监听本机。若改为非本机地址，必须配置 `standalone_webui_token`，否则插件会拒绝启动该监听。两种入口共用同一套 `_dashboard` 前端和任务 API。

控制台提供仪表盘、任务配置、工作流画布、任务管理、记忆管理、插件与集成等完整操作界面。

### 任务结束与管理员审批

- 完成条件和运行证据满足时，Bot 可以主动完成并归档。
- 完成校验存在缺口时，系统会创建 `finish_override` 管理员审批，而不是伪装成完成成功。
- 若配置了 `workflow_admin_ids`，聊天中的批准/拒绝只允许这些管理员执行。
- 管理员批准结束后，任务会自动归档，并在 `finish_decisions` 与报告记录中保存批准人、被豁免证据和最终总结。
- 「停止并归档」会安全关闭心跳、恢复会话插件并保存取消记录；「彻底删除」会在安全停止后删除归档和关联任务记忆。

## 命令

| 命令 | 说明 |
|------|------|
| `/al status` | 查看当前任务状态 |
| `/al start <目标>` | 开启新任务 |
| `/al tick` | 推进任务一步 |
| `/al finish <总结>` | 完成并归档 |
| `/al cancel <原因>` | 取消当前任务 |
| `/al use <id>` | 切换默认 Agent 配置 |
| `/al agents` | 列出所有 Agent 配置 |
| `/al heartbeat on/off` | 开关心跳 |
| `/al approve/reject <id>` | 审批操作 |
| `/al webui` | 获取控制台地址 |

完整前缀 `/agentlab` 同样可用。

## 触发策略

可在 WebUI 中为每个 Agent 配置配置触发模式：

- **手动** — 只通过命令或 WebUI 开启
- **确认** — bot 判断合适时先请求确认（默认）
- **智能** — 低风险自动进入，高风险确认
- **自动** — 尽可能使用任务模式，高风险仍需审批

## 配置项

| 配置 | 说明 | 默认 |
|------|------|------|
| `standalone_webui_enabled` | 启用独立控制台 | `true` |
| `standalone_webui_host` | 监听地址 | `127.0.0.1` |
| `standalone_webui_port` | 端口 | `8788` |
| `standalone_webui_token` | 独立控制台访问 Token；非本机监听必填 | 空 |

## 兼容性

- AstrBot >= 4.16, < 5
- 默认仅私聊可用
- Bot 身份自动继承 AstrBot 运行时 Persona 配置

## 自检

```bash
python -m compileall -q .
python scripts/smoke_test.py
```

## 许可证

见 [LICENSE](LICENSE) 文件。
